# This workflow is disabled - deploy.yml handles deployment
# Kept for reference only
name: Frontend CI (disabled)

on:
  workflow_dispatch: # Manual only

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use deploy.yml workflow instead"
            pm2 save
